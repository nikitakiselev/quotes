# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=quotes_user
DB_PASSWORD=quotes_password
DB_NAME=quotes_db
DB_SSLMODE=disable

# API Configuration
API_PORT=8080
CORS_ORIGIN=http://localhost:3000

# Frontend Configuration
# Порт, на котором будет доступен основной сайт
FRONTEND_PORT=3000
# Оставляем пустым для использования относительных путей (рекомендуется)
# Для локальной разработки можно задать http://localhost:8080
VITE_API_URL=

# Admin Configuration
# Порт, на котором будет доступна административная панель
ADMIN_PORT=3001
# Пароль для входа в административную панель (измените на свой!)
ADMIN_PASSWORD=admin
# URL API для админки (оставляем пустым для относительных путей)
ADMIN_API_URL=
